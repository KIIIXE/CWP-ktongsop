def checkmate(board):
บรรทัดนี้: ประกาศฟังก์ชันชื่อ checkmate โดยรับค่า board (กระดานหมากรุกที่เป็นข้อความยาวๆ) เข้ามา

Python    # 1. แยกกระดานออกเป็นบรรทัดๆ
    lines = board.split('\n')
บรรทัดนี้: คำสั่ง .split('\n') จะตัดข้อความ board ออกเป็นส่วนๆ ทุกครั้งที่เจอการขึ้นบรรทัดใหม่ (\n) แล้วเก็บผลลัพธ์เป็นรายการ (List) ชื่อ lines

Python    # เอาบรรทัดที่ว่างเปล่าออก (ป้องกันข้อผิดพลาดเวลาตัดคำ)
    rows = []
    for line in lines:
        if line != "":
            rows.append(line)
โค้ดส่วนนี้:สร้างลิสต์เปล่าชื่อ rows ขึ้นมาเตรียมไว้วนลูปอ่านทีละบรรทัด (line) จากลิสต์ linesเช็คว่าบรรทัดนั้นไม่ใช่บรรทัดว่าง (line != "")ถ้าไม่ว่าง ก็ให้เอาบรรทัดนั้นไปต่อท้ายใน rows (ขั้นตอนนี้ทำเพื่อความปลอดภัย เผื่อข้อความมีบรรทัดว่างเกินมา)

Python    # 2. เช็คว่ากระดานว่างไหม และขนาดเป็นสี่เหลี่ยมจัตุรัสหรือเปล่า
    size = len(rows)
    if size == 0:
        return
โค้ดส่วนนี้: นับจำนวนบรรทัดของกระดาน (len(rows)) แล้วเก็บไว้ในตัวแปร size ถ้ากระดานว่างเปล่า (size == 0) ให้จบการทำงานของฟังก์ชันไปเลย (return)

Python    for row in rows:
        if len(row) != size:
            print("Error")
            return
โค้ดส่วนนี้: ตรวจสอบกฎที่ว่ากระดานต้องเป็นสี่เหลี่ยมจัตุรัส โดยวนลูปเช็คความยาวตัวอักษรของแต่ละบรรทัด (len(row)) ถ้ามีความยาวไม่เท่ากับจำนวนบรรทัด (size) แปลว่าไม่ใช่สี่เหลี่ยมจัตุรัส ให้พิมพ์คำว่า "Error" แล้วจบการทำงานทันที   

Python    # 3. หาตำแหน่งของ King (K)
    king_x = -1
    king_y = -1
บรรทัดนี้: สร้างตัวแปร king_x (หมายเลขแถว) และ king_y (หมายเลขคอลัมน์) เพื่อเตรียมเก็บตำแหน่งของ King ตั้งค่าเริ่มต้นเป็น -1 ไว้ก่อน (หมายความว่ายังหาไม่เจอ)

Python    for i in range(size):
        for j in range(size):
            if rows[i][j] == 'K':
                king_x = i
                king_y = j
โค้ดส่วนนี้: ใช้ลูปซ้อนลูปเพื่อสแกนกระดานทีละช่อง:ลูป i (แถวที่ 0 ถึง แถวสุดท้าย)ลูป j (คอลัมน์ที่ 0 ถึง คอลัมน์สุดท้ายของแถวนั้นๆ)ถ้าพบตัวอักษร 'K' ในช่องนั้น (rows[i][j]) ก็ให้จำตำแหน่งแถว (i) ไว้ใน king_x และจำคอลัมน์ (j) ไว้ใน king_y

Python    # ถ้าหา King ไม่เจอ
    if king_x == -1:
        print("Error")
        return
โค้ดส่วนนี้: หลังสแกนจบทั้งกระดานแล้ว ถ้า king_x ยังเป็น -1 แสดงว่าไม่มี King อยู่บนกระดานเลย ให้พิมพ์ "Error" และจบการทำงาน

Python    # 4. กำหนดทิศทางทั้ง 8 ทิศ
    directions = [
        (-1, 0), (1, 0), (0, -1), (0, 1),   # บน, ล่าง, ซ้าย, ขวา (แนวตรง)
        (-1, -1), (-1, 1), (1, -1), (1, 1)  # เฉียง 4 มุม (แนวทแยง)
    ]
โค้ดส่วนนี้: สร้างลิสต์เก็บพิกัดทิศทางทั้ง 8 ตัวเลขคือ (เปลี่ยนแถว, เปลี่ยนคอลัมน์) เช่น (-1, 0) คือ ขยับแถวขึ้น 1 แต่คอลัมน์เท่าเดิม (มองขึ้นบน)

Python    # 5. มองออกไปทีละทิศทางจากตัว King
    for d in directions:
        move_x = d[0]
        move_y = d[1]
โค้ดส่วนนี้: วนลูปหยิบทิศทางจากตัวแปร directions มาทีละคู่ โดยแยกเป็น move_x (การเปลี่ยนแถว) และ move_y (การเปลี่ยนคอลัมน์)Python        x = king_x + move_x
        y = king_y + move_y
        distance = 1 # ระยะห่างจาก King
โค้ดส่วนนี้: กำหนดจุดเริ่มต้นในการมองคือ ขยับออกจากตัว King ไป 1 ช่อง ตามทิศทางนั้นๆ และตั้งค่า distance เริ่มต้นที่ 1 (เพื่อเอาไว้วัดระยะการโจมตีของ Pawn)

Python        # เดินไปเรื่อยๆ จนกว่าจะสุดขอบกระดาน
        while x >= 0 and x < size and y >= 0 and y < size:
            piece = rows[x][y]
โค้ดส่วนนี้: ลูป while จะทำงานไปเรื่อยๆ ตราบใดที่ตำแหน่งสายตา (x, y) ยังอยู่ภายในขอบเขตกระดาน (ไม่น้อยกว่า 0 และไม่เกิน size) จากนั้นดึงตัวอักษรในช่องนั้นมาเก็บในตัวแปร piece

Python            # ถ้าเจอหมากตัวอื่น (ไม่ใช่ช่องว่าง หรือจุด)
            if piece != '.' and piece != ' ':
บรรทัดนี้: กฎบอกว่าหมากแต่ละตัวสามารถกินได้แค่หมากตัวแรกสุดที่เจอ ดังนั้นถ้าช่องนั้นไม่ใช่จุด (.) หรือช่องว่าง ( ) แสดงว่าเราเจอหมากตัวแรกที่ขวางทางอยู่แล้ว   

Python                # เช็คว่าเป็นแนวตรงหรือแนวทแยง
                is_straight = (move_x == 0 or move_y == 0)
                is_diagonal = (move_x != 0 and move_y != 0)
โค้ดส่วนนี้: เช็คว่าทิศทางที่เรามองออกมานี้ เป็นแนวตรง (แถวหรือคอลัมน์ไม่เปลี่ยน) หรือแนวทแยง (เปลี่ยนทั้งคู่)

Python                # ตรวจสอบว่าหมากตัวนั้นรุก King ได้ไหม
                if piece == 'Q':
                    print("Success")
                    return
โค้ดส่วนนี้: ถ้าหมากตัวแรกที่เจอคือ Queen (Q) ซึ่งเดินได้ทุกทิศทาง ก็แปลว่ารุก King ได้แน่นอน พิมพ์ "Success" แล้วจบการทำงานทันที   

Python                elif piece == 'R' and is_straight == True:
                    print("Success")
                    return
โค้ดส่วนนี้: ถ้าเจอ Rook (R) และเรามองมาใน is_straight (แนวตรง) Rook สามารถโจมตีได้ จึงรุก King ได้ ให้พิมพ์ "Success" แล้วจบการทำงาน   

Python                elif piece == 'B' and is_diagonal == True:
                    print("Success")
                    return
โค้ดส่วนนี้: ถ้าเจอ Bishop (B) และเรามองมาใน is_diagonal (แนวทแยง) Bishop โจมตีแนวนี้ได้ จึงรุก King ได้ พิมพ์ "Success" แล้วจบการทำงาน   

Python                elif piece == 'P' and is_diagonal == True and distance == 1:
                    print("Success")
                    return
โค้ดส่วนนี้: ถ้าเจอ Pawn (P) จะต้องเป็นแนวทแยง (is_diagonal) และต้องอยู่ห่างจาก King แค่ 1 ช่องเท่านั้น (distance == 1) จึงจะถือว่าโจมตีถึง ถ้าเงื่อนไขครบ พิมพ์ "Success" แล้วจบการทำงาน

Python                # ถ้าเจอหมากที่รุกไม่ได้ (หรือฝ่ายเดียวกันบังอยู่) ให้หยุดมองทิศนี้
                break
บรรทัดนี้: ถ้าเจอหมากตัวอื่นที่เงื่อนไขการรุกไม่ตรง (เช่น เจอ Rook แต่เป็นแนวทแยง) หมากตัวนั้นก็จะทำหน้าที่เป็น "กำแพงบัง" ให้ King เราจึงหยุดมองทิศนี้ (break ออกจากลูป while) เพื่อเริ่มมองทิศทางต่อไป

Python            # ขยับไปช่องถัดไปในทิศเดิม
            x = x + move_x
            y = y + move_y
            distance = distance + 1
โค้ดส่วนนี้: (ส่วนนี้อยู่นอก if piece != ...) ถ้าช่องที่เรามองยังเป็นช่องว่าง เราก็แค่ขยับสายตาออกไปอีก 1 ช่องในทิศทางเดิม และบวกระยะห่าง distance เพิ่มขึ้น 1

Python    # ถ้ารอดทุกทิศทาง ไม่มีใครรุกถึง
    print("Fail")
บรรทัดสุดท้าย: ถ้าวนลูปจนครบทั้ง 8 ทิศทางแล้ว และยังไม่โดนจับกินเลย (ไม่เคยเข้าเงื่อนไขพิมพ์ "Success") โปรแกรมก็จะหลุดมาถึงบรรทัดนี้ แปลว่า King ปลอดภัย ให้พิมพ์ "Fail" ครับ